const handleProducts = (getProduct)=>{
    // const res = getData();
    data.filter((res)=>{
        if(itm.name == getProduct){
            setSrchRes(itm)
        }
    })
}

export {handleProducts}